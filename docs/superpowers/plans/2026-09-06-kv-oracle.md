# KV Oracle Diagnostic Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 独立测量无卸载开销的 KV 容量收益，并给出工程继续/停止建议。

**Architecture:** 给现有 BenchmarkBackend 增加必要且默认行为不变的参数构建扩展点；OracleBackend 调用同一原生生成/计时循环。独立标准库 evidence/suite 拒绝伪装正式收益。worker root-module hook 只读统计前向和可用请求数。

**Tech Stack:** Python3.10+、现有 PyTorch2.8/vLLM0.10.2、pytest、标准库；无新依赖。

**Spec:** docs/superpowers/specs/2026-09-06-kv-oracle.md

## Global Constraints

- Worktree `/Users/a1234/code/flexmoe/.worktrees/repro-fluxmoe`, branch `repro/fluxmoe`, pulled server base `7cc2145`.
- Existing Qwen3-Next-80B-A3B BF16, TP4, four exclusive H100; original weights/routing, no new weights/data/dependencies.
- Only GitHub sync and exact pinned Docker Hub image fetch may use server network; build RUN steps stay offline.
- Server writes only `/home/jovyan/wangtonghan/moe-flex`; public weights/data read-only.
- Do not change expert cache/policy/pool or CUDA kernels. Preserve old benchmarks, strict fairness guards and server smoke fallback in export.
- Oracle is diagnostic-only and may exceed baseline budget; no formal offload-gain flag or success wording, including on positive synthetic fixtures.
- TDD, apply_patch edits, scoped commits; CPU tests are not H100 evidence. No push by subagents.

### Task 1: Integrated native KV Oracle workflow

**Files:** Create `src/flexmoe/bench/kv_oracle_runner.py`, `kv_oracle_evidence.py`, `kv_oracle_suite.py`; `src/flexmoe/vllm/native_probe.py`; `scripts/server/run_kv_oracle.sh`; `docs/kv-oracle-runbook.md`, `docs/server-codex-kv-oracle-prompt.md`; focused `tests/unit/test_kv_oracle.py`, `test_native_probe.py`. Modify `BenchmarkBackend`/`run_benchmark` in `partial_runner.py` only through small explicit hooks, add native probe RPC to `bridge.py`, formal Oracle rejection in `partial_suite.py`, and README links. Additional focused tests may be split by responsibility if useful; no new generic framework.

**Interfaces:**
- Preserve `engine_arguments(config, fixed_kv_bytes)` restriction: normal native resident override must raise. Add `BenchmarkBackend.engine_arguments(config, fixed_kv_bytes)` default forwarding if needed; OracleBackend alone builds native args then explicitly sets target KV. Persist actual requested override in summary rather than old local variable.
- `OracleBackend(role, baseline_reference, small_add_bytes, large_add_bytes, safety_fraction, safety_margin_bytes)` consumes validated completed native r0 reference for O roles. Reuse shared `run_benchmark` without masquerading as partial-fixed-kv. It may do independent post-run reference validation instead of weakening shared reference contract. Store role outside common workload contract.
- Worker RPC `fluxmoe_native_probe(action)` supports start/stop/snapshot. Actual model hook; no GPU data access in hook; rank-local fixed-size counters and availability flags, plus CUDA-parameter residency checks outside timing. Missing runner request telemetry is null, never guessed. Pair start/stop removes hooks and avoids double counting. Existing memory peak RPC is reused.
- Native `screen|confirm|export|point` entrypoints; host `kv_oracle_suite.py` stays stdlib-only even when run via `python -S`. Roles r0/small/large all storage native; forward and reverse with explicit anchor as spec.
- Independent numeric `validate/analyze/export` with `diagnostic_only=true`, `formal_offload_gain=false`; old formal analyzer rejects Oracle backend explicitly.

- [ ] **RED: add budget/isolation tests before implementation.** Use hand-derived 4-rank fixture with allocated KV=1000, available=1200 and small increment=200; target must be1200, not1400. A small/large or incomplete reference cannot act as r0 anchor. Non-finite/negative inputs and unsafe targets reject. Existing native override remains forbidden.

```python
with pytest.raises(ValueError):
    engine_arguments(native_config, 1000)
# With a real OracleBackend and a complete r0 fixture, inspect native args:
assert oracle_args['kv_cache_memory_bytes'] == 1200
assert oracle_args['cpu_offload_gb'] == 0
assert oracle_args['enforce_eager'] is True
```

- [ ] **GREEN: implement dedicated budget construction and safety checks.** Share model/workload/version/actual-memory helpers; preserve old default behavior. Explicit estimated-safety scope and actual capacity checks, no silent target reduction. Label nominal baseline budget accurately for O groups.
- [ ] **RED/GREEN: native probe and shared-loop integration.** Drive a real small `torch.nn.Module` to show start/stop only measures intended calls, changing input_batch.num_reqs updates observed peak, missing fields remain unavailable and no duplicate hook occurs. Oracle R/O calls use native engine, fixed tokens, same input/engine settings, per-rep peak resets and complete smoke/seconds accounting. Mock only actual vLLM/CUDA boundary, not the orchestration.
- [ ] **RED/GREEN: complete diagnostic evidence and decisions.** Legal fixture r0=100s/small=80s/large=70s with full corresponding token counts produces diagnostic ratios and time savings only; no formal gain. Missing rank/probe/counter/memory fields, mismatched input/model/SHA/engine, altered KV, bogus tokens/s, failed/OOM points and overlapping repetition ranges cannot produce a confirmed decision. Probe data unavailable means mechanism unknown, not proof of no benefit. Test raw and sanitized validation paths and legacy formal rejection.
- [ ] **RED/GREEN: host suite, ordering, failure export.** Run stdlib help in subprocess; inspect real generated argv for custom model/dataset and all numeric flags; forward order r0/small/large, reverse uses prior eligible anchor but validates against newly measured final r0. Different anchor repetitions allowed only for capacity derivation, never substitute its speed. Whole-point timeout inside Docker; failed/no-summary/smoke-only states retained and exported. Fresh IDs, no destructive cleanup or hidden retry.
- [ ] **Docs and server prompt.** Chinese actionable workflow: clean branch/pull/build, exclusive GPUs, existing model/data; screen control/main, choose one meaningful point for three-rep forward/reverse confirmation, long only conditionally. Report outcomes before considering offload. Strong Oracle-over-budget warning, estimated safety vs actual peak, null unavailable metrics, scalar GPU/KV amounts in decimal GB, no performance promise. Private paths in public commands use variables. Commit fixes before rebuilding; export negative/incomplete cases; push only code and sanitized results. Link from README.
- [ ] **Verify and commit.** Targeted tests, all CPU unit/integration tests with local pinned vLLM source, full-source Ruff/Mypy, shell syntax and stdlib CLI help, wheel build, git diff --check. Report all skipped hardware work and no H100 conclusions. Commit task files and write report with RED/GREEN/API/commands/limitations to task report path.

## Completion

- [ ] Independent task review and scoped fixes, then whole-change review.
- [ ] Fresh verification, push existing branch (user explicitly requested), verify remote SHA and provide server prompt.
